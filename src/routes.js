import CinemaListPage from './Component/CinemaListPage';

const routes = [
    {
      name: 'cinema',
      path: '/cinemas',
      description: 'Cinemas',
      component: CinemaListPage
    }
  ];

  export default routes;